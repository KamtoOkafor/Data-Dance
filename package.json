{
  "name": "FinalProjectImplementation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently -n 'server,client' -c 'magenta,green' \"cd backend && npm start\" \"cd frontend && npm start\"",
    "dev": "concurrently -n 'server,client' -c 'magenta,green' \"cd backend && npm run dev\" \"cd frontend && npm start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.0.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
